name: velorki_loops
description: Smart loop candidate strategies and scoring for Velorki. Pure Dart, no Flutter dependency.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.3

dependencies:
  velorki_brouter:
    path: ../velorki_brouter
  velorki_geo:
    path: ../velorki_geo

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
