/// Package version, kept in sync with pubspec.yaml.
const String packageVersion = '0.1.0';

/// The BRouter tag this package is a port of. Must equal the contents of
/// `brouter/UPSTREAM_VERSION` in the repository (the Docker image and the test
/// oracle are pinned to the same tag).
const String upstreamVersion = 'v1.7.10';
