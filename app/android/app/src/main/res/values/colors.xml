<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Written by flutter_launcher_icons from pubspec.yaml; the light icon
         tile, the volt accent's light tone (AccentPreset.volt in
         lib/app/theme.dart). -->
    <color name="ic_launcher_background">#3F7A00</color>
    <!-- The launch splash: the light icon, full screen. The window behind the
         Flutter UI while the engine starts, so the app opens on the icon's
         own colour instead of a white flash. values-night/colors.xml carries
         the dark pair; nothing else here is night-dependent. -->
    <color name="velorki_splash_background">#3F7A00</color>
    <color name="velorki_splash_glyph">#FFFFFF</color>
</resources>
