<?xml version="1.0" encoding="utf-8"?>
<!-- The launch splash: a flat fill in the app's seed colour. The layer-list
     is kept (rather than pointing windowBackground straight at the colour) so
     a launch image can be added on top later. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/velorki_splash_background" />
</layer-list>
