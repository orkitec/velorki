<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Map tiles, routing and search all go over the network. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- The position puck and ride recording. "When in use" only: background
         location is deliberately not requested, see docs/ARCHITECTURE.md. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Ride recording: foreground service with the location type, plus the
         notification that keeps it visible (Android 13+) and a wake lock while
         recording. No ACCESS_BACKGROUND_LOCATION on purpose: the service is
         started while the app is in the foreground. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Health Connect, only ever used after the rider switches Health on in
         Settings → Sensors: heart rate is read to show and store with a ride,
         and a finished ride is written back as a cycling workout (an exercise
         session with its distance). Declaring a permission asks for nothing;
         the Health Connect sheet is only raised by that switch. See the
         `health` package README, "Google Health Connect (Android)". -->
    <uses-permission android:name="android.permission.health.READ_HEART_RATE"/>
    <uses-permission android:name="android.permission.health.WRITE_EXERCISE"/>
    <uses-permission android:name="android.permission.health.WRITE_DISTANCE"/>
    <!-- Bluetooth sensors: heart-rate straps, speed and cadence sensors and
         power meters, only ever used after the rider pairs one on
         Settings → Sensors → Bluetooth sensors. The scan is declared
         neverForLocation because Velorki reads the three cycling profiles and
         never the position of a beacon; the runtime prompt is raised by the
         Scan button on that screen and nowhere else. BLUETOOTH and
         BLUETOOTH_ADMIN are the pre-Android 12 spelling of the same thing and
         are capped there. No ACCESS_FINE_LOCATION is added for the legacy
         scan: the recorder already declares it above, uncapped. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <!-- Not required: a phone without Bluetooth Low Energy installs and runs,
         it simply finds no sensors. -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="false" />
    <!-- Rides, routes and the OAuth tokens in the keystore stay on the device:
         android:allowBackup="false" keeps them out of Google's cloud backup
         (and out of any backup at all on Android 11 and below, which is why no
         android:fullBackupContent rules are needed). Android 12+ ignores
         allowBackup for device-to-device transfer, so @xml/data_extraction_rules
         excludes everything there as well. -->
    <application
        android:label="Velorki"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Shared route links (velorki://share/<id>). A custom scheme
                 needs no android:autoVerify. The OAuth host is deliberately
                 left to flutter_web_auth_2's CallbackActivity below: two
                 activities matching the same URI would make Android ask the
                 user which one should open it. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <!-- The oauth host belongs to flutter_web_auth_2's
                     CallbackActivity. -->
                <data android:scheme="velorki" android:host="share"/>
            </intent-filter>
            <!-- The same share links as https (App Links):
                 https://velorki.com/s/<id>, with or without .gpx.
                 android:autoVerify makes Android check
                 https://velorki.com/.well-known/assetlinks.json on install and
                 open the app without a chooser. That file is served by the
                 website from its ANDROID_CERT_SHA256 setting, the release
                 keystore's SHA-256 fingerprint; until it is live the link
                 opens in the browser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https" android:host="velorki.com"
                      android:pathPrefix="/s/"/>
            </intent-filter>
            <!-- Open GPX and FIT files from file managers, mail, browsers and
                 the share sheet. Senders are inconsistent about MIME types, so
                 octet-stream with a path pattern is matched too; the app sniffs
                 the content before trusting the type. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/gpx" />
                <data android:mimeType="application/vnd.ant.fit" />
                <data android:mimeType="application/fit" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
                <data android:mimeType="text/plain" />
                <data android:pathPattern=".*\\.gpx" />
                <data android:pathPattern=".*\\..*\\.gpx" />
                <data android:pathPattern=".*\\.fit" />
                <data android:pathPattern=".*\\..*\\.fit" />
                <data android:pathPattern=".*\\.GPX" />
                <data android:pathPattern=".*\\.FIT" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/gpx+xml" />
                <data android:mimeType="application/gpx" />
                <data android:mimeType="application/vnd.ant.fit" />
                <data android:mimeType="application/fit" />
                <data android:mimeType="application/octet-stream" />
                <data android:mimeType="application/xml" />
                <data android:mimeType="text/xml" />
            </intent-filter>
        </activity>
        <!-- Health Connect links to the app from its permission screen, and
             requires the target to be exported behind START_VIEW_PERMISSION_USAGE.
             It points at MainActivity, which opens the app's own screens; the
             privacy policy is reachable from Settings → About. Required setup
             from the `health` package README. -->
        <activity-alias
            android:name="ViewPermissionUsageActivity"
            android:exported="true"
            android:targetActivity=".MainActivity"
            android:permission="android.permission.START_VIEW_PERMISSION_USAGE">
            <intent-filter>
                <action android:name="android.intent.action.VIEW_PERMISSION_USAGE" />
                <category android:name="android.intent.category.HEALTH_PERMISSIONS" />
            </intent-filter>
        </activity-alias>
        <!-- flutter_web_auth_2: catches velorki://oauth/<service>?code=...
             and hands it back to the pending authentication session. Required
             by the plugin; android:exported and the empty android:taskAffinity
             are its documented setup. -->
        <activity
            android:name="com.linusu.flutter_web_auth_2.CallbackActivity"
            android:exported="true"
            android:taskAffinity="">
            <intent-filter android:label="flutter_web_auth_2">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="velorki" android:host="oauth" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <!-- flutter_foreground_task: do not rename. -->
        <service
            android:name="com.pravera.flutter_foreground_task.service.ForegroundService"
            android:foregroundServiceType="location"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <!-- url_launcher: open https links (Android 11+ package visibility). -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- flutter_tts: find the installed text-to-speech engines for the
             spoken turn directions (Android 11+ package visibility). -->
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE"/>
        </intent>
        <!-- health: see whether Health Connect is installed at all, and reach
             its permissions rationale. Package visibility only; nothing is
             read or asked for until the rider switches Health on. Required
             setup from the `health` package README. -->
        <package android:name="com.google.android.apps.healthdata" />
        <intent>
            <action android:name="androidx.health.ACTION_SHOW_PERMISSIONS_RATIONALE" />
        </intent>
    </queries>
</manifest>
