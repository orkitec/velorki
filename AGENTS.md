# Agents

Read `CLAUDE.md` first. It holds the working rules for this repository:
verify on a device before committing, no AI attribution in commits, the
toolchain facts, the code conventions and where the tests live. Everything
in it applies to every coding agent, whatever tool runs it.
