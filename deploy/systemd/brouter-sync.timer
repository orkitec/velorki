[Unit]
Description=Weekly BRouter segment mirror (Velorki)

[Timer]
OnCalendar=Sun 03:00
# Catch up if the machine was off at the scheduled time.
Persistent=true
# Spread load on brouter.de - it is a courtesy mirror.
RandomizedDelaySec=2h
Unit=brouter-sync.service

[Install]
WantedBy=timers.target
